[package]
name = "quanta-core-rs"
version = "0.1.0"
edition = "2021"

[dependencies]
bitcode = "0.6"
serde = { version = "1", features = ["derive"] }

[dev-dependencies]
criterion = { version = "0.5", features = ["html_reports"] }

[[bench]]
name = "delta_bench"
harness = false
