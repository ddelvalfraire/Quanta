{
  "name": "particle-world",
  "private": true,
  "type": "module",
  "scripts": {
    "build:wasm": "cd ../../rust/quanta-wasm-decoder && wasm-pack build --target web --out-dir ../../examples/particle-world/wasm-decoder",
    "dev": "npm run build:wasm && vite",
    "build": "npm run build:wasm && tsc && vite build",
    "preview": "vite preview"
  },
  "devDependencies": {
    "typescript": "^5.4.0",
    "vite": "^6.0.0",
    "vite-plugin-wasm": "^3.6.0"
  },
  "dependencies": {
    "pixi.js": "^8.18.1"
  }
}
